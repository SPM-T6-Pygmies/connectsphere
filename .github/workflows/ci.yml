name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  checks:
    name: Quality checks
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Migration versions are unique
        if: ${{ always() }}
        continue-on-error: false
        run: |
          duplicates=$(ls supabase/migrations/*.sql | xargs -n1 basename | cut -d_ -f1 | sort | uniq -d)
          if [ -n "$duplicates" ]; then
            echo "::error::Duplicate migration versions (rename one file in each pair):" $duplicates
            exit 1
          fi

      - name: Type check
        if: ${{ always() }}
        continue-on-error: false
        run: pnpm typecheck

      - name: Lint
        if: ${{ always() }}
        continue-on-error: false
        run: pnpm lint

      - name: Tests and registry
        if: ${{ always() }}
        continue-on-error: false
        run: pnpm test:report --check

      - name: Build
        if: ${{ always() }}
        continue-on-error: false
        run: pnpm build

  record:
    name: Record passing tests
    needs: checks
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          # Full history so the rebase before pushing has something to rebase onto.
          fetch-depth: 0
          # GITHUB_TOKEN cannot push to main: the basic-protection ruleset puts a
          # pull_request rule on it, and GitHub Actions is built in rather than an
          # installed org app, so it cannot be named as a bypass actor. This is an
          # admin's fine-grained PAT (Contents: read and write, this repo only),
          # and the ruleset lets the admin role bypass.
          # One-time setup: docs/tests/README.md#recording-runs.
          token: ${{ secrets.TEST_RECORD_TOKEN }}

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Record the run
        run: pnpm test:report --record

      - name: Commit the record
        env:
          HAS_TOKEN: ${{ secrets.TEST_RECORD_TOKEN != '' }}
        run: |
          if [ "$HAS_TOKEN" != "true" ]; then
            echo "::error title=Missing secret::TEST_RECORD_TOKEN is not set, so the run cannot be recorded. See docs/tests/README.md."
            exit 1
          fi
          if git diff --quiet docs/tests; then
            echo "Nothing changed; not committing."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/tests
          # [skip ci] keeps this push from retriggering the workflow.
          git commit -m "Record passing tests for ${GITHUB_SHA:0:7} [skip ci]"
          git pull --rebase --autostash
          git push
